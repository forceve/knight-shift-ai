import React from "react";

// The Knobs scene uses SVG/CSS for the main visuals (Knobs + Cards).
// The background (AmbientScene) remains visible.
// This component is a placeholder for any 3D-specific elements if needed later.
export default function KnobsScene() {
  return null;
}
